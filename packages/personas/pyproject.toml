[project]
name = "heimdall-personas"
version = "0.0.1"
description = "BRP persona registry and prompt fragments. Stub: full library populated in Week 2."
requires-python = ">=3.12,<3.13"
dependencies = [
  "heimdall-contracts",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/heimdall_personas"]
